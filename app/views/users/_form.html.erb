<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :blog_name %>
    <%= f.text_field :blog_name %>
  </div>
  <div class="field">
    <%= f.label :user_name %>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :password_plain %>
    <%= f.password_field :password_plain %>
  </div>
  <div class="field">
    <%= f.label :password_plain_confirmation %>
    <%= f.password_field :password_plain_confirmation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
